# Mini-Projet_Contenairization-Cloud

#Q1_Dockerfile_Odoo / Q2_Dockerfile_PostgreSQL
  Les commandes pour la partie une sont : 
    sudo Docker build -t [name-image] .
    sudo docker run 
  Et pour verifier si l'image a bien été créée
    sudo docker images 
